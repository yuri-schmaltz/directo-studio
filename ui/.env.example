# Directo UI — environment variables
# Copy to .env.local and adjust

# URL of the FastAPI backend (no trailing slash)
DIRECTO_API_URL=http://localhost:8000

# WebSocket URL (for live event stream)
# If different from DIRECTO_API_URL, set explicitly
NEXT_PUBLIC_DIRECTO_WS_URL=ws://localhost:8000
